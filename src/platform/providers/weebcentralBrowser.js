// Bundles the unmodified weebcentral manga extension for use inside native apps.
// Same approach as anikotoBrowser.js — Vite's CJS plugin + cheerio/slim alias.
import provider from '../../../extensions/Manga/weebcentral.cjs';

export default provider;
